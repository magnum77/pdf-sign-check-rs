services:
  pdf-sign-check:
    build:
      context: .
    image: pdf-sign-check-rs:local
    environment:
      PDF_SIGN_CHECK_BIND: ${PDF_SIGN_CHECK_BIND:-0.0.0.0:3000}
    ports:
      - "${PDF_SIGN_CHECK_PORT:-3000}:3000"
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://127.0.0.1:3000/healthz >/dev/null || exit 1"]
      interval: 10s
      timeout: 3s
      retries: 3
      start_period: 20s
